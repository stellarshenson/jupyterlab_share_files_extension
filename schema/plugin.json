{
  "title": "Share Files",
  "description": "Settings for the Share Files extension. Toggles take effect immediately - no reload required.",
  "type": "object",
  "properties": {
    "enableShares": {
      "type": "boolean",
      "default": true,
      "title": "Enable file sharing",
      "description": "Show the 'My Shares' section, the 'Share Files...' context menu in the file browser, and the 'New Share' command. When off, you can still receive files via connections."
    },
    "enableRequests": {
      "type": "boolean",
      "default": true,
      "title": "Enable file requests",
      "description": "Show the 'My Requests' section and the 'New Request' command. When off, you can still upload to other people's requests via connections."
    },
    "showHiddenFiles": {
      "type": "boolean",
      "default": true,
      "title": "Show hidden files",
      "description": "Show files and folders whose name starts with a dot (.env, .ssh, .ipynb_checkpoints, ...). Useful for sharing config files between team members."
    },
    "pollIntervalSeconds": {
      "type": "number",
      "default": 15,
      "minimum": 2,
      "title": "Refresh interval (seconds)",
      "description": "How often the panel polls the server for changes - new uploads to your requests, updated connections, and all of your shares and requests. Lower values refresh faster but make more requests. Minimum 2 seconds."
    }
  },
  "additionalProperties": false
}
